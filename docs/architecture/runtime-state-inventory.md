# Runtime Mutable State Inventory

本清单是 Runtime 数据生命周期政策登记表。表名使用真实表或明确的表族；动态业务对象表由 `business-records` 行统一登记，但 owner 在执行清理时仍必须按具体 Object policy 裁决。Identity 账号、认证、角色与授权状态不属于 Runtime：module 模式由 `domainry-identity` 的独立数据库持有，SaaS 模式由远程 Identity 服务持有。`Retention class` 只能是 `product_retention`、`legal_audit_retention`、`technical_ttl` 或 `user_requested_erase`。

| State / table family | Authoritative owner | Workspace scope | Sensitivity / region | Source of truth | Policy key | Retention class | Retention / minimum | Archive + purge / legal hold | Backup behavior | Export / erase | Loss allowed | Consistency | Capacity policy | Multi-instance target |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `business-records` (dynamic object tables, including soft-deleted rows) | record | workspace column | object policy; PII/financial may apply; workspace region | database | `record.object.default.v1` plus object override | product_retention | active lifetime + object policy / 30d after eligible | owner policy; active workflow/outbox/reference and legal hold block purge | standard restore then reconcile | subject export; delete or anonymize by field policy | No | transactional aggregate | paginated; cleanup job required | shared durable repository |
| `_audit_events` | audit | workspace column | audit/security; workspace region | database | `audit.evidence.v1` | legal_audit_retention | 7y / 1y | immutable archive; legal hold blocks archive removal and purge | compliance locked | controlled audit export; anonymize subject fields, never destroy event integrity | No | append-only | paginated; archive partitions/object storage required | shared durable repository |
| `workflow_definition_identities`, `workflow_definition_versions` | workflow | definition scope; installation default until workspace column migration | internal/audit | database | `workflow.definition.v1` | product_retention | active + 2y archived / 1y | archived version purge blocked while a process references its snapshot | standard restore then reconcile | configuration export; no subject erase | No | versioned append history | paginated version history; cleanup by reference | shared durable repository |
| `workflow_process_instances`, `workflow_node_instances`, `workflow_tasks`, `workflow_process_events`, `_workflow_executions` | workflow | workspace column | business/PII/audit; workspace region | database | `workflow.execution.v1` | legal_audit_retention | completed + 7y / 1y | terminal-only archive; active process, record reference, outbox and legal hold block purge | compliance locked | subject export; anonymize comments/variables where evidence permits | No | durable state machine | unbounded history: archive and batch purge required | shared durable repository |
| `workflow_execution_receipts` | workflow | workspace column | internal/security | database | `workflow.receipt.v1` | technical_ttl | replay window + 30d / replay window | expired terminal receipts only; referenced execution blocks purge | standard restore then reconcile | not exported; delete after expiry | No before replay window | fenced claim/complete | expiry cleanup | shared durable repository |
| `automation_rule_executions`, `automation_instruction_executions` | automation | workspace column | business/audit | database | `automation.execution.v1` | product_retention | completed + 1y / 90d | terminal-only archive; active lease, record reference and legal hold block purge | standard restore then reconcile | subject export; anonymize payload fields by object policy | No while replayable | fenced execution | unbounded history: batch archive/purge required | shared durable repository |
| scheduler definitions, runs, attempts and dead letters stored through record owner tables | scheduler | workspace column | business/audit | database | `scheduler.execution.v1` | product_retention | success 180d; failure/dead letter 1y / 90d | terminal-only; pending run, retry, compensation, reference and legal hold block purge | standard restore then reconcile | controlled export; erase follows referenced record policy | No while runnable/replayable | fenced durable job | unbounded history: status-specific cleanup required | shared durable repository |
| `business_action_executions`, `record_mutation_executions`, `business_change_plan_operations` | action/record/changeplan | workspace column | business/security | database | `execution.idempotency_receipt.v1` | technical_ttl | provider/business replay window + 30d / replay window | terminal and expired only; referenced result blocks purge | standard restore then reconcile | not subject export except linked result; delete after expiry | No before replay window | atomic receipt | expiry cleanup already required | shared durable repository |
| `runtime_operations` | operations | workspace or explicit system purpose | operator/audit/security | database | `operations.receipt.v1` | legal_audit_retention | completed + 1y; failed/manual 7y / 90d | terminal-only archive; ticket, evidence, active owner execution and legal hold block purge | compliance locked | controlled redacted operator export; never erase audit identity without tombstone | No | atomic idempotent registration and fenced status transition | paginated status index; archive/purge job required | shared durable operation ledger |
| `runtime_operation_controls` | operations | explicit system purpose | operator/security | database | `operations.control.v1` | legal_audit_retention | superseded revisions represented by operation receipts; current desired state retained | delete only after referenced operation evidence and deployment retention expire | configuration/control state included in encrypted backup and restore drill | controlled operator export; no user erasure | No | revision-fenced desired state; every instance polls shared row and mutation fails closed when unavailable | bounded by registered control kinds and worker owners | shared durable maintenance, owner-pause, and instance-drain state |
| `runtime_break_glass_grants` | operations/security | workspace column with Runtime-global execution purpose | operator/security/incident | database | `operations.break_glass.v1` | legal_audit_retention | grant expiry + 7y audit / 1y | only expired or revoked grants; incident/legal hold and referenced Operations receipt block purge | compliance locked and included in restore drill | controlled redacted incident export; approver/audit identity retained | No | serializable single-active registration and revision-fenced revoke; expiry is always checked at use/read time | bounded active grant and paginated history | shared durable grant store with alert/audit evidence |
| `record_batch_jobs`, `record_batch_job_chunks` | record | workspace column | business/PII | database + generated result file | `record.batch_artifact.v1` | product_retention | result 7d; job evidence 90d / 24h | delete chunks/file before minimal job evidence; active lease and legal hold block purge | delayed erase after restore | authorized export; expired download and token deleted together | No while active | fenced checkpoint | bounded batch/chunk size; orphan cleanup | shared durable repository/object storage |
| `lifecycle_file_artifacts` and workspace upload directories | upload/file | workspace column plus hashed workspace directory | business/possible PII | database registry + local/object storage | `file.upload.v1` | product_retention | staged 24h; referenced for record lifetime; orphan grace 24h / 1h staging safety | record reference and legal hold preserve file; registry proves orphan before deletion and retains hash/size/status evidence | delayed erase after restore | governed record subject export includes declared files; erase clears record field and deletes file | No while referenced | durable registry reconciled by lifecycle worker | content upload bounded to 5 MiB; staged/orphan cleanup indexed | shared registry with configured storage backend |
| `integration_connections`, `integration_webhook_subscriptions`, `web_push_subscriptions` | integration | workspace column | sensitive configuration | database | `integration.configuration.v1` | product_retention | active + 1y / 90d | revoke/disable first; active outbox/reference and legal hold block purge | delayed erase after restore | redacted configuration export; erase endpoint, browser keys, and secret references | No | versioned mutation | bounded per workspace | shared durable repository |
| `integration_secret_materials`, `integration_secrets`, `integration_api_keys` | integration | workspace column | security; residency follows workspace/provider contract | encrypted database | `integration.secret.v1` | user_requested_erase | active + rotation/replay window / 30d | revoke first; purge ciphertext only after dependent jobs/outbox expire; legal hold stores metadata, not plaintext | delayed erase after restore | never export secret value; delete ciphertext and retain redacted audit | No while active | atomic rotation | bounded versions; rotation cleanup | shared durable repository |
| `integration_external_identities`, `integration_event_mapping_intents` | integration | workspace column | PII | database | `integration.identity_mapping.v1` | user_requested_erase | active mapping + 90d / 30d | provider reconciliation and legal hold can require anonymized tombstone | delayed erase after restore | subject export; delete provider mapping or anonymize tombstone | No while mapping active | durable mapping | paginated; reconciliation cleanup | shared durable repository |
| `integration_events` | integration | workspace column | provider payload may contain PII/security | database | `integration.event.v1` | product_retention | succeeded 90d; failed/dead 1y / dedup window | terminal-only; mapping, retry, outbox, audit reference and legal hold block purge | delayed erase after restore | controlled subject export; redact/anonymize payload | No while dedup/retry active | fenced inbox | unbounded event history: status cleanup required | shared durable repository |
| `integration_invocations`, `integration_outbox_messages`, `transaction_boundary_intents` | integration/transaction | workspace column | business/security/audit | database | `integration.delivery_evidence.v1` | legal_audit_retention | completed 1y; failed/compensation 7y / provider reconcile window | terminal and reconciled only; outbox/reference/compensation/legal hold block purge | compliance locked | redacted evidence export; anonymize payload while retaining request refs | No while reconcilable | fenced delivery/compensation | unbounded history: archive and purge required | shared durable repository |
| `integration_webhook_nonces` | integration | workspace column | security | database | `integration.webhook_nonce.v1` | technical_ttl | signature replay window + 1h / replay window | no archive; expired-only purge | standard restore then reconcile | not exported; delete after expiry | No before expiry | unique nonce | expiry cleanup, separate from event dedup | shared durable repository |
| `integration_credential_refresh_leases`, `idempotency_cleanup_leases`, notification publication locks | owning integration/idempotency/notification owner | workspace where present; otherwise installation singleton | security/internal | database | `technical.lease_checkpoint.v1` | technical_ttl | completed + 7d / max lease + recovery window | no archive; lost/active lease and incomplete checkpoint block purge | standard restore then reconcile | not exported; delete completed technical state | No while active | fenced lease/checkpoint | bounded singleton/keyed rows | shared durable coordination |
| `metadata_catalog`, `metadata_definition_versions`, all `*_definitions` tables | metadata/definition | installation or resource workspace contract | internal/audit | database | `metadata.definition_history.v1` | product_retention | active + 7y version history / 1y | referenced version and legal hold block purge; archive remains queryable | compliance locked | configuration export; no subject erase | No | append/versioned | unbounded versions: reference-aware archive required | shared durable repository |
| notification template records/versions/publication requests/policy/preferences/reservations | notification | workspace where present; global template keys otherwise | preferences may be PII; publication audit | database | `notification.history.v1` | product_retention | versions 2y; requests/reservations 180d / 90d | active publication/dedupe reference and legal hold block purge | standard restore then reconcile | preference subject export/erase; retain anonymized publication evidence | No while referenced | versioned/fenced publication | unbounded versions/reservations: cleanup required | shared durable repository |
| `business_localized_text` | localization | workspace column | business/possible PII | database | `localization.text.v1` | product_retention | source lifetime + 90d / 30d | source reference and legal hold block purge | standard restore then reconcile | included with source export/erase | No while source active | unique localized key | paginated; source-driven cleanup | shared durable repository |
| `business_record_localized_value` | record | workspace + object + record | business/possible PII | database | `record.localized_value.v1` | product_retention | owning record lifetime | owning record deletion and legal hold govern cleanup | restore owning record and translations together | included with owning-record export/erase | No while source active | unique workspace/object/record/field/locale | batched by record IDs; locale-aware search index | record mutation transaction |
| `_business_seed_provenance`, `frontend_capability_manifests` | businessseed/capability | installation/workspace as declared | internal | database | `runtime.configuration.v1` | product_retention | current + 1y superseded / 90d | referenced deployment blocks purge; archive version before replacement | standard restore then reconcile | admin configuration export; no subject erase | No | revisioned configuration | bounded revisions required | shared durable repository |
| `_schema_migrations`, `_runtime_schema_migrations`, `_runtime_rls_policies`, `metadata_exact_decimal_migrations` | persistence | installation/schema | audit/security | database | `persistence.migration_evidence.v1` | legal_audit_retention | installation lifetime / installation lifetime | never automatic purge; legal hold implicit | compliance locked | operator-only export; not erasable | No | append-only/checksummed | bounded by releases/policies | shared database authority |
| `runtime_rate_limit_bucket` | ratelimit | workspace/key scope | security/internal | database | `ratelimit.bucket.v1` | technical_ttl | window + 1h / window | no archive; expired bucket only | standard restore then reconcile | not exported; delete after expiry | Yes after window | atomic bucket update | expiry cleanup | shared durable limiter |
| `agent_runtime_state`, `agentDialogSessions`, `agentDialogProposals` | agent | workspace/user/session scope | PII/business | database | `agent.dialog.v1` | product_retention | accepted session 1y; proposals 180d / 30d | archived session only; referenced report/action and legal hold block purge | delayed erase after restore | subject export; redact/anonymize dialogue content | No after acceptance | durable per session/revision | pagination and archive cleanup | shared durable repository |
| `agentReportQueryRuns`, `agentReportExportAudits`, `agentReportDownloadTasks` | report | workspace/user scope | PII/audit | database + result file | `report.download.v1` + `report.export.v1` | technical_ttl + legal_audit_retention | download 7d / 1d; audit 1y / 90d | expire token and file together; legal hold preserves redacted evidence | delayed erase after restore | authorized export; subject erase removes result and anonymizes audit | No while referenced | durable task transitions | bounded queue; file cleanup and audit archive | shared durable queue/repository |
| `dictionaryCache` | record | instance local, keyed by workspace | internal | durable metadata is authoritative | `cache.dictionary.v1` | technical_ttl | metadata invalidation or 15m / 1m | no archive; evict freely | not backed up | not exported; evict | Yes | best-effort cache | bounded by dictionary catalog | instance-local deterministic refill |
| request, localization, manifest and permission process caches | platform/owning projection | instance local, workspace-keyed where applicable | internal/security | durable owner state is authoritative | `cache.runtime_projection.v1` | technical_ttl | invalidation or 15m / 1m | no archive; evict freely | not backed up | not exported; evict | Yes | reconstructable projection | bounded LRU/owner catalog | instance-local deterministic refill |

## Policy defaults and override boundary

- Every production state uses a versioned policy key. Policy publication must record owner, class, default and minimum retention, sensitivity, backup behavior, erase behavior, reference checks and legal-hold eligibility.
- A workspace may extend a policy only where the policy allows it. A workspace may never reduce `legal_audit_retention`, security/audit evidence, or any declared minimum retention.
- Product retention is driven by owner lifecycle state. Technical TTL is driven by expiry plus replay/lease safety. A user erase request is an independently verified workflow and never silently overrides legal/audit retention.
- Backup erase is delayed: a restored backup must replay the deletion registry before the restored Runtime is exposed. Online purge completion does not claim immediate removal from compliance-locked backups.
- Region follows workspace placement unless a provider contract declares an additional region. Cross-region archive/export requires an explicit provider and workspace policy decision.

## Unbounded-growth enforcement closure

No production collection in this inventory remains both unbounded and without an enforcement mode. The fenced lifecycle worker now covers Audit; Workflow definition/process/execution/receipt state with child-before-parent ordering; Automation; Scheduler runs/events/dead letters; Integration event/invocation/outbox/transaction-boundary evidence/nonces/expired leases; Operations receipts and expired Break-glass grants; action/record idempotency receipts; soft-deleted dynamic business records; record batch chunks/jobs; Metadata versions; Notification versions/publication requests/delivery reservations; Agent dialog state; Report download/export/query state; and expired rate-limit buckets. The upload registry separately reconciles final file references, observes orphan grace, deletes mature orphans, and retains hash/size/status evidence; the same worker removes interrupted `.upload-*` staging files and expired subject exports.

Policies for source-driven localization, current Runtime configuration, installation-lifetime migration evidence, and reconstructable caches are explicitly classified as subject-governed, bounded-current, permanent evidence, or cache eviction rather than generic purge. `TestEveryDefaultRetentionPolicyHasAnExplicitEnforcementMode` fails when a new default policy lacks one of these reviewed modes.

Any newly introduced mutable collection must be classified here before release. Durable business or audit state must not depend on process memory; local caches must remain reconstructable and safe under multi-instance divergence.
