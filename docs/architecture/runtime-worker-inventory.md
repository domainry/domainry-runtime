# Runtime Worker Inventory

This inventory is the production-reachability record for Runtime background execution. It classifies technical execution facts without moving runnable, terminal, retryable, or failure semantics out of each business owner.

## Inventory contract

Every process loop or durable task must record: owner, entrypoint, durable store, atomic claim, lease TTL, heartbeat, retry, dead letter, cancellation, recovery, and current migration gap. `domainry-foundation/worker` owns only process lifecycle and the shared `Lease`, `FencingToken`, `HeartbeatResult`, `ShutdownState`, `Clock`, `WorkerID`, and `JitterSource` contracts.

| Worker ID | Business owner | Production entrypoint | Durable store / claim | Lease TTL / heartbeat | Retry / DLQ | Cancel / recovery | Current gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `scheduler` | domainry-scheduler | Bootstrap `startSchedulerWorker` -> Scheduler SDK `Binding.Start` | Scheduler-owned `_scheduler_definition_states`, `_scheduler_runs`, immutable run events, and dead letters | Scheduler policy TTL; heartbeat and fencing are implemented inside the Scheduler module/SaaS owner | Scheduler owns capped retry and terminal dead letters; Runtime Operations delegates through the SDK binding | shared Runtime worker admission wraps Module mode; SaaS owns its clock worker; retry/cancel/reschedule remain owner commands | Runtime contains no recurrence cursor, run, run-event, or Scheduler dead-letter object |
| `record_timer` | Runtime Record Timer | Bootstrap `startRecordTimerWorker` -> `recordtimer.RecordTimerApplicationService.ProcessDueForAllWorkspaces` | Runtime-owned `record_timer`; ordered optimistic claim with lease owner, expiry and fencing token; immutable `record_timer_event` is committed with execution transitions | `RECORD_TIMER_LEASE_TTL`; bounded work uses fencing and expired-lease reclaim | exponential capped retry updates `due_at`; exhaustion sets `failed`; Operations delegates retry/resolve/ack to the `record_timer` owner | shared pause/drain admission and context cancellation; retry resets the attempt budget and appends a `requeued` event | worker config is independent `RECORD_TIMER_*`; terminal timers archive/purge child events through Record lifecycle policy and never reuse Scheduler state |
| `agent_task` | Agent | Agent Module/SaaS Binding worker; Runtime only invokes SDK `TaskRunner.Start` and receives the later Host callback | Agent-owned `_agent_task_runs` and `_agent_worker_scopes`; direct wake plus durable scoped recovery use Agent repository claim/CAS and fencing | Agent-owned lease TTL and heartbeat; Agent worker identity + fencing token | Agent owns capped exponential retry, provider reconciliation and durable `dead_letter` | Binding lifecycle cancellation; lost wakeups and expired leases recover from Agent state, and uncertain Provider Start polls the durable external run ID before a new Start | terminal Agent state commits before the idempotent Runtime Workflow callback; Runtime runs no Agent worker and writes no Agent task row |
| `workflow_execution` | Workflow | authenticated Scheduler callback for scheduled definitions, recovery worker, and `POST /workflow-tasks/process-due` | `_workflow_execution_receipts` protects request idempotency; due `_workflow_executions` use conditional claim CAS | 5 minutes; heartbeat every minute; process WorkerID + fencing token | Workflow owns replay, retryable, and `dead_letter` results; generic operations delegates retry/resolve/ack back to Workflow | shared pause/drain admission; expired execution/receipt reclaim increments token and preserves evidence | SQLite/PostgreSQL/MySQL concurrency contract verified with real databases on 2026-07-19 |
| `workflow_deadline` | Workflow / Record Timer | Record Timer `workflow:approval_deadline` target and manual process-due operation | `record_timer` determines due delivery; `_workflow_tasks` owns the idempotent reminder/escalation transition | Record Timer lease is independent; Workflow transition is bounded and has no second heartbeat | Workflow owns idempotent reminder/escalation event keys; Record Timer owns delivery retry/failure | Record Timer expired-lease reclaim plus Workflow task fencing reject stale transitions | deadline execution remains on the Workflow aggregate; no generic Scheduler Job is created |
| `automation_instruction` | Automation | Automation instruction execution use case | `_automation_instruction_executions`; `AutomationWorkerStore.ClaimInstruction` performs atomic insert/CAS claim | 3 minutes; heartbeat every 1 minute; process WorkerID + fencing token | Automation owns step failure state; no separate DLQ | request context cancellation; expired Lease reclaim | request-triggered execution, not a process polling loop |
| `integration_event` | Integration | Runtime lifecycle calls SDK `LocalWorkers.ProcessDueEvents`; Integration SaaS starts the same owner loop itself | Integration-owned `_integration_events` claim, lease/fencing, mapping intent and execution receipt | owner policy; expired leases are reclaimable | Integration owns bounded retry and durable `dead_letter`; Runtime only returns Action/Workflow receipts through `TriggerSink` | host lifecycle cancellation stops the loop; durable owner state recovers restart and failed triggers | Runtime schedules the embedded loop but contains no event worker policy or event persistence |
| `runtime_publication_outbox` | Runtime publication handoff | `bootstrap/runtime.StartPublicationHandoffWorker` | committed `workspace_id + message_id` is routed through a bounded local broker to an exact Workspace-scoped `_publication_outbox` read and conditional claim; at most 8 executors call `Integration Delivery.Accept`; 30-second recovery scans durable due work | repository lease; heartbeat every 90 seconds; process WorkerID + fencing token | bounded retry of the idempotent owner acceptance boundary; terminal `dead_letter` after the Runtime handoff attempt limit | shared pause/drain admission; local wakeups are lossy hints, while recovery handles retry, expired lease, restart and cross-node commits; Runtime never interprets Provider outcome | `_publication_outbox` is the only Runtime publication handoff fact; stable message/deduplication identity is preserved across retries and the returned owner reference is stored only as `response_ref` |
| `integration_reconciliation` | Integration | Runtime lifecycle calls SDK `LocalWorkers.ProcessDueReconciliations`; Integration SaaS owns its loop | Integration-owned `_integration_invocations` conditional claim and reconciliation metadata | stale `reconciling` facts are reclaimable after the owner timeout | Integration applies the Connector reliability contract and persists terminal/pending outcomes | host lifecycle cancellation; durable invocation metadata drives restart recovery | Runtime contains no reconciliation policy or Provider lookup implementation |
| `integration_credentials` | Integration | Runtime lifecycle calls SDK `LocalWorkers.ProcessDueCredentialExpirations`; Integration SaaS owns its loop | Integration-owned `_integration_secrets`; compare-and-set `active -> expired` | bounded owner scan; no cross-owner lease needed for the idempotent state transition | next scan retries uncommitted candidates | host lifecycle cancellation and durable status recovery | notification of upcoming expiry is a separate owner integration, not Runtime credential policy |
| `integration_provider` | Integration | Runtime lifecycle calls SDK `LocalWorkers.ProcessDueProviderTasks`; Integration SaaS owns its loop | Integration-owned `_integration_connector_provider_states` and `_integration_connector_provider_commits`; conditional lease/fencing; Provider state is committed before normalized events and post-commit effects | owner lease and expired-lease reclaim | Integration owns retry, secret updates, event durability and Provider-commit DLQ; Connector Provider owns protocol state transitions | host lifecycle cancellation; durable state and commit queue recover restart | one generic owner loop serves Connector implementations from `domainry-connectors` |
| `workflow_continuation` | Workflow / Agent | Bootstrap continuation worker | committed `workspace_id + execution_id` wakes an exact `_workflow_executions` read and owner CAS claim; a 30-second recovery pass rotates through the payload-free Workspace registry for retry, deadline, expired Lease, restart and lost-wakeup recovery | five-minute execution Lease with heartbeat; process WorkerID + fencing token | Workflow owns bounded retry/dead-letter semantics and `NextRunAt` | shared pause/drain admission; wakeups are lossy local hints, while durable recovery and fencing provide cluster correctness | scheduled Workflow callbacks create Workflow-owned executions; continuation consumes retry/process-resume executions without a second business queue |
| `notification_publication` | Notification | `bootstrap/runtime.StartNotificationPublicationWorker` | committed `request_id` wake performs exact claim on `_notification_template_publication_requests`; a 30-second due/expired-Lease recovery pass uses the same conditional claim and increments fencing token | 2 minutes; short publication has no heartbeat; process WorkerID + fencing token | Notification owns published/failed/superseded terminal states; failed/superseded are terminal, not automatically retried | shared pause/drain admission; local wakeups are lossy hints; recovery covers cross-node commits, restart and expired publication; stale terminal writer rejected | renderer convergence is a separate per-node 5-second lightweight revision check; failure retry/DLQ owner policy remains intentionally minimal |
| `notification_renderer_refresh` | Notification local projection | joined lifecycle inside `bootstrap/runtime.StartNotificationPublicationWorker` | durable notification publication revision; no claim because every Runtime node must refresh its own in-memory renderer | not applicable; 5-second revision check, full template reload only when revision changes | next revision check retries read/reload; no DLQ is appropriate for a local cache | shared pause/drain admission and context cancellation; startup load plus revision polling recover current state | intentionally not a separate business task class or durable queue; it is the local-projection half split from notification publication execution |
| `notification_inbox` | Notification | Bootstrap `startNotificationInboxWorker`; every ordinary or cross-owner producer returns after durable event commit | committed `workspace_id + event_id` wakes an exact Workspace-scoped claim on `_notification_events`; a 30-second recovery pass rotates through the payload-free Workspace registry; item projection, channel plans and event completion commit atomically | 30 seconds; each materialization batch is bounded and has no heartbeat; process WorkerID + fencing token | Notification owns exponential retry; every failed claim appends a safe `_notification_event_failures` attempt in the same transaction as retry/dead-letter transition; source identity makes replay idempotent | shared pause/drain admission; local wakeups are lossy hints; recovery covers cross-node commits, restart and expired processing Lease; stale terminal writer is rejected and deterministic per-user item IDs prevent duplicates | channel plan commit emits exact locators consumed by item 07; audience materialization remains capped at 500 recipients per event |
| `notification_channel` | Notification / Runtime publication handoff | Bootstrap `startNotificationChannelWorker` | committed `workspace_id + plan_id` wakes an exact Workspace-scoped read and conditional claim for immediate plans; a 30-second recovery pass aggregates due Digest windows before inserting one stable-dedup `integration.connector` row into `_publication_outbox` through the host gateway | 30 seconds; bounded render-and-plan work has no heartbeat; process WorkerID + fencing token | Notification owns plan retry; Runtime publication handoff owns only idempotent Integration acceptance retry; Integration owns Provider delivery and DLQ | shared pause/drain admission; database CAS/lease/fencing arbitrates clusters, while plan ID or deterministic Digest window ID is the publication dedupe identity | terminal exhausted plan governance and per-channel operator metrics remain to be completed |
| `data_exchange` | domainry-data-exchange | Bootstrap `startDataExchangeWorker` -> Data Exchange SDK `Binding.Start` | Module mode owns `_data_exchange_jobs`, `_data_exchange_job_chunks`, `_data_exchange_artifacts`, and `_data_exchange_queue_scopes`; SaaS mode owns equivalent remote state | Binding-owned lease TTL; heartbeat at TTL / 3; owner runtime/application identity + fencing token | Data Exchange owns terminal processing failure; provider code owns Record/Report validation and completion effects | shared Runtime worker admission wraps Module mode; SaaS owns its worker lifecycle; owner-scoped job reads/cancel/download prevent cross-provider access | Runtime contains no import/export queue, chunk, artifact, lease, retry, dead-letter, or notification store |
| `idempotency_cleanup` | Deployment runtime status | Bootstrap cleanup worker | cleanup lease in durable runtime status store; `RunIdempotencyCleanup` performs atomic acquisition | 2 minutes; tick-scoped work, no heartbeat | next tick retries; no DLQ required for bounded cleanup | shared pause/drain admission; expired cleanup Lease reclaim | no remaining worker dependency gap |
| `lifecycle_cleanup` | Lifecycle | `bootstrap/runtime.startLifecycleCleanupWorker` | `_lifecycle_cleanup_jobs`; workspace-scoped conditional claim increments fencing token | 2 minutes; each batch is bounded and the next claim resumes from durable status | failed jobs remain durable and retry after Lease expiry; evidence records the failure | context cancellation; archive-before-purge makes partial batches resumable | owner executors cover Runtime-owned Audit, Workflow, Automation, Integration, Operations, Auth/Rate-limit, Action, Record, Metadata, Notification and Report state; Agent, Data Exchange and Scheduler retention stay with their module/SaaS owners |
| `metadata_snapshot` | Metadata | `bootstrap/runtime.New` starts a 5-second snapshot watcher | durable metadata snapshot revision; no claim because every instance must refresh its local projection | not applicable | next tick retries read/reload | context cancellation; process restart reloads current revision | one fixed-key revision row per tick; intentionally executes on every instance |
| `runtime_release_cohort` | Deployment | generated project Runtime startup and `bootstrap/runtime.startRuntimeReleaseHeartbeat` | `_release_cohorts` serializes the active generation; `_release_instances` records one renewable lease per process | 45 seconds; heartbeat every 15 seconds; Runtime instance ID + cohort generation fence | startup retries only SQL serialization/lock races; identity conflicts fail closed and are not retried | caller context cancellation; clean host shutdown releases immediately, crash recovery waits for expiry; lost heartbeat irreversibly closes local traffic admission | real SQLite concurrent first-claim, matching join, conflict, expiry and stale-generation fencing are covered; opt-in real MySQL/PostgreSQL transaction suite remains the external dialect gate |

## Non-worker goroutines

- `cmd/server` runs `http.Server.ListenAndServe` in a lifecycle goroutine; it does not claim durable work.
- `domainry-foundation/worker.WithHeartbeat` owns the shared per-claim heartbeat goroutine.
- Module mode Data Exchange owns its polling and per-claim heartbeat goroutines behind `Binding.Start`; Runtime owns only lifecycle admission.
- Connector plugins do not start provider-owned polling loops; `plugins.StartBackgroundWorkers` intentionally delegates durable delivery to Integration event/outbox workers.
- Notification Inbox SSE owns request-scoped poll and heartbeat tickers; both stop with the request context, do not claim durable work, and only emit invalidation cursors.

## Reviewed raw ticker baseline

Process polling loops must use `domainry-foundation/worker.StartNamedLoop`. The only reviewed production `time.NewTicker` call sites are:

- `domainry-foundation/worker/lifecycle.go`: shared named loop implementation;
- `domainry-foundation/worker/heartbeat.go`: shared per-claim heartbeat implementation;
- `runtime/transport/http/records/records_stream_handler.go`: request-scoped content-free Business Workspace SSE invalidation polling and heartbeat; not a process worker.

Adding another raw ticker requires updating this inventory and its architecture gate in the same reviewed change.
