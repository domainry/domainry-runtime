# Runtime Worker Inventory

This inventory is the production-reachability record for Runtime background execution. It classifies technical execution facts without moving runnable, terminal, retryable, or failure semantics out of each business owner.

## Inventory contract

Every process loop or durable task must record: owner, entrypoint, durable store, atomic claim, lease TTL, heartbeat, retry, dead letter, cancellation, recovery, and current migration gap. `platform/worker` owns only process lifecycle and the shared `Lease`, `FencingToken`, `HeartbeatResult`, `ShutdownState`, `Clock`, `WorkerID`, and `JitterSource` contracts.

| Worker ID | Business owner | Production entrypoint | Durable store / claim | Lease TTL / heartbeat | Retry / DLQ | Cancel / recovery | Current gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `scheduler` | Scheduler | `bootstrap/runtime.StartWorkflowWorker` -> Scheduler `StartWorker` | `job_run` records; `claimRun` uses conditional create / `UpdateRecordWhere` reclaim by definition window | owner policy TTL; heartbeat at TTL / 3; process WorkerID + fencing token | Scheduler owns capped retry and durable `job_dead_letter`; generic operations delegates resolve/ack back to Scheduler | shared pause/drain admission, context cancellation; cancel increments the token and expired run reclaim allocates a new token | generic Record CAS is the owner adapter; its SQLite/PostgreSQL/MySQL concurrency contract is shared with Scheduler |
| `agent_task` | Agent | Bootstrap `startAgentTaskWorker` when the Agent HTTP provider is explicitly configured | committed `workspace_id + task_id` wakes a direct Workspace-scoped claim; a 30-second recovery pass rotates through the payload-free Workspace registry for retry, expired Lease, restart and lost-wakeup recovery; both paths use the owner table's serializable conditional claim and increment the fencing token | configured Scheduler Lease TTL; heartbeat at TTL / 3; process WorkerID + fencing token | Agent owns capped exponential retry and durable `dead_letter`; typed provider error classes decide retryability | shared pause/drain admission and request cancellation; wakeups are lossy local hints, while durable recovery and fencing provide cluster correctness; expired Lease reclaim polls the durable external run ID before any new Start | forced PostgreSQL RLS is entered before Workspace task reads/claims; SQLite/PostgreSQL/MySQL share the same owner-store dialect gate |
| `workflow_execution` | Workflow | Scheduler tick and `POST /workflow-tasks/process-due` | `workflow_execution_receipts` protects request idempotency; due `_workflow_executions` use conditional claim CAS | 5 minutes; heartbeat every minute; process WorkerID + fencing token | Workflow owns replay, retryable, and `dead_letter` results; generic operations delegates retry/resolve/ack back to Workflow | shared pause/drain admission; expired execution/receipt reclaim increments token and preserves evidence | SQLite/PostgreSQL/MySQL concurrency contract verified with real databases on 2026-07-19 |
| `workflow_deadline` | Workflow | Scheduler tick and manual process-due operation | `workflow_tasks`; due rows are claimed by one conditional update that increments the fencing token | 5 minutes; bounded reminder/escalation pass has no heartbeat | Workflow owns idempotent reminder/escalation event keys | shared pause/drain admission; expired Lease reclaim increments the token and stale completion is rejected | deadline execution remains on the Workflow-owned task aggregate rather than creating a generic Job model |
| `automation_instruction` | Automation | Automation instruction execution use case | `automation_instruction_executions`; `AutomationWorkerStore.ClaimInstruction` performs atomic insert/CAS claim | 3 minutes; heartbeat every 1 minute; process WorkerID + fencing token | Automation owns step failure state; no separate DLQ | request context cancellation; expired Lease reclaim | request-triggered execution, not a process polling loop |
| `integration_event` | Integration | `bootstrap/runtime.StartIntegrationEventWorker` | committed `workspace_id + event_id` wakes an exact Workspace-scoped `integration_events` read and conditional claim CAS; a 30-second recovery pass rotates through the payload-free Workspace registry | 5 minutes; heartbeat every 90 seconds; process WorkerID + fencing token | shared failure classes mapped by Integration; jittered bounded retry; durable `dead_letter` / `quarantined` | shared pause/drain admission; wakeups are lossy local hints, while recovery handles retry, expired Lease, restart and cross-node commits | Webhook returns after durable acceptance and existing receipt/identity writes, not handler execution; cluster ownership remains CAS/lease/fencing across SQLite/PostgreSQL/MySQL |
| `integration_outbox` | Integration | `bootstrap/runtime.StartIntegrationOutboxWorker` | committed `workspace_id + outbox_id` is routed through a bounded local broker to an exact Workspace-scoped read and conditional claim; at most 8 executors send concurrently; 30-second recovery rotates through the payload-free Workspace registry | 5 minutes; heartbeat every 90 seconds; process WorkerID + fencing token | shared failure classes mapped by Integration; jittered bounded retry; durable `dead_letter` / `quarantined` / reconciliation evidence | shared pause/drain admission; local wakeups are lossy hints, while recovery handles retry, expired Lease, restart and cross-node commits; provider request reference survives reclaim | `integration_outbox_messages` is the only task fact; the legacy per-message `integration_outbox_task` index is removed and Record transaction inserts publish only after commit |
| `integration_invocation_reconciliation` | Integration | Bootstrap reconciliation worker | durable prepared integration invocations; one conditional `prepared -> reconciliation_required` update is both claim and final handoff | not applicable: no external work occurs between claim and terminal handoff | next scan retries untouched prepared facts; reconciliation-required is the poison quarantine | shared pause/drain admission and context cancellation; the single CAS prevents duplicate terminal evidence | intentionally no renewable Lease because this worker performs no post-claim side effect |
| `integration_credential_expiry` | Integration / Notification | Bootstrap `startIntegrationCredentialExpiryWorker` | Integration-owned Workspace credential metadata is scanned from the Runtime-global system scope; stable source-event and dedupe identities are committed through the Integration/Notification transaction adapter before Notification claims the event | hourly bounded scan; no claim or heartbeat because the scan only performs idempotent transactional intents; downstream `notification_inbox` owns the durable event Lease | the next hourly scan retries candidates whose intent transaction failed; stable 30/7/1/expired milestone identities prevent duplicate events and Notification owns materialization retry/DLQ | shared pause/drain admission and context cancellation; process restart rescans durable expiry metadata, and credential recovery uses the same alert group | terminal scan failure metrics and configurable scan cadence remain to be completed |
| `connector_provider_background` | Integration | Runtime reconciles optional task declarations from every active Connector Provider; Google declares `gmail_sync` and, when enabled, `gmail_watch` | generic `connector_provider_states` due scan and conditional lease claim/fencing; provider payload is opaque versioned JSON | 2 minutes; every provider transition is bounded | Runtime owns retries, secrets, event durability and post-durable commit operations; Provider owns protocol cursors and transitions | expired leases are reclaimed; task declarations are reconciled at startup and connection changes | one generic loop serves all modules; Gmail History and Pub/Sub semantics live only in the Google Provider |
| `workflow_continuation` | Workflow / Agent | Bootstrap continuation worker | committed `workspace_id + execution_id` wakes an exact `_workflow_executions` read and owner CAS claim; a 30-second recovery pass rotates through the payload-free Workspace registry for retry, deadline, expired Lease, restart and lost-wakeup recovery | five-minute execution Lease with heartbeat; process WorkerID + fencing token | Workflow owns bounded retry/dead-letter semantics and `NextRunAt` | shared pause/drain admission; wakeups are lossy local hints, while durable recovery and fencing provide cluster correctness | Scheduler still owns creation of scheduled executions; continuation consumes retry/process-resume executions without a second business queue |
| `notification_publication` | Notification | `bootstrap/runtime.StartNotificationPublicationWorker` | committed `request_id` wake performs exact claim on `notification_template_publication_requests`; a 30-second due/expired-Lease recovery pass uses the same conditional claim and increments fencing token | 2 minutes; short publication has no heartbeat; process WorkerID + fencing token | Notification owns published/failed/superseded terminal states; failed/superseded are terminal, not automatically retried | shared pause/drain admission; local wakeups are lossy hints; recovery covers cross-node commits, restart and expired publication; stale terminal writer rejected | renderer convergence is a separate per-node 5-second lightweight revision check; failure retry/DLQ owner policy remains intentionally minimal |
| `notification_renderer_refresh` | Notification local projection | joined lifecycle inside `bootstrap/runtime.StartNotificationPublicationWorker` | durable notification publication revision; no claim because every Runtime node must refresh its own in-memory renderer | not applicable; 5-second revision check, full template reload only when revision changes | next revision check retries read/reload; no DLQ is appropriate for a local cache | shared pause/drain admission and context cancellation; startup load plus revision polling recover current state | intentionally not a separate business task class or durable queue; it is the local-projection half split from notification publication execution |
| `notification_inbox` | Notification | Bootstrap `startNotificationInboxWorker`; every ordinary or cross-owner producer returns after durable event commit | committed `workspace_id + event_id` wakes an exact Workspace-scoped claim on `notification_events`; a 30-second recovery pass rotates through the payload-free Workspace registry; item projection, channel plans and event completion commit atomically | 30 seconds; each materialization batch is bounded and has no heartbeat; process WorkerID + fencing token | Notification owns exponential retry; every failed claim appends a safe `notification_event_failures` attempt in the same transaction as retry/dead-letter transition; source identity makes replay idempotent | shared pause/drain admission; local wakeups are lossy hints; recovery covers cross-node commits, restart and expired processing Lease; stale terminal writer is rejected and deterministic per-user item IDs prevent duplicates | channel plan commit emits exact locators consumed by item 07; audience materialization remains capped at 500 recipients per event |
| `notification_channel` | Notification / Integration | Bootstrap `startNotificationChannelWorker` | committed `workspace_id + plan_id` wakes an exact Workspace-scoped read and conditional claim for immediate plans; a 30-second recovery pass reads only the payload-free Workspace registry globally, enters each Workspace, and aggregates due Digest windows with Workspace included in the grouping key before inserting one stable-dedup Integration Outbox message | 30 seconds; bounded render-and-plan work has no heartbeat; process WorkerID + fencing token | Notification owns exponential plan retry; Digest locators never send a single item early; Integration Outbox owns provider delivery retry and DLQ independently | shared pause/drain admission; lossy local wakeups are only acceleration; database CAS/Lease/fencing arbitrates clusters, while plan ID (or deterministic Digest window ID) is the Outbox dedupe identity across crash-after-insert recovery | terminal exhausted plan governance and per-channel operator metrics remain to be completed |
| `record_batch` | Record | Bootstrap record batch worker; committed enqueue/requeue publishes a process-local wakeup | `record_batch_jobs`; wakeup interrupts adaptive backoff, while `ClaimRecordBatchJobs` remains the durable stable-order conditional claim | 1 minute; heartbeat every 20 seconds; process WorkerID + fencing token | Record owns three attempts, exponential delay + injected jitter, exhausted `failed` evidence, and terminal poison `quarantined`; generic operations delegates retry/resolve/ack back to Record | shared pause/drain admission; adaptive recovery remains for cross-node/lost wakeups, retries and expired Lease; persisted cancel polling every 2 seconds | SQLite/PostgreSQL/MySQL concurrency contract verified with real databases on 2026-07-19 |
| `idempotency_cleanup` | Deployment runtime status | Bootstrap cleanup worker | cleanup lease in durable runtime status store; `RunIdempotencyCleanup` performs atomic acquisition | 2 minutes; tick-scoped work, no heartbeat | next tick retries; no DLQ required for bounded cleanup | shared pause/drain admission; expired cleanup Lease reclaim | no remaining worker dependency gap |
| `lifecycle_cleanup` | Lifecycle | `bootstrap/runtime.startLifecycleCleanupWorker` | `lifecycle_cleanup_jobs`; workspace-scoped conditional claim increments fencing token | 2 minutes; each batch is bounded and the next claim resumes from durable status | failed jobs remain durable and retry after Lease expiry; evidence records the failure | context cancellation; archive-before-purge makes partial batches resumable | owner executors cover registered Audit, Workflow, Automation, Scheduler, Integration, Operations, Auth/Rate-limit, Action, Record, Metadata, Notification, Agent and Report state; the same loop reconciles upload/file artifacts |
| `metadata_snapshot` | Metadata | `bootstrap/runtime.New` starts a 5-second snapshot watcher | durable metadata snapshot revision; no claim because every instance must refresh its local projection | not applicable | next tick retries read/reload | context cancellation; process restart reloads current revision | one fixed-key revision row per tick; intentionally executes on every instance |
| `runtime_release_cohort` | Deployment | generated project Runtime startup and `bootstrap/runtime.startRuntimeReleaseHeartbeat` | `runtime_release_cohorts` serializes the active generation; `runtime_release_instances` records one renewable lease per process | 45 seconds; heartbeat every 15 seconds; Runtime instance ID + cohort generation fence | startup retries only SQL serialization/lock races; identity conflicts fail closed and are not retried | caller context cancellation; clean host shutdown releases immediately, crash recovery waits for expiry; lost heartbeat irreversibly closes local traffic admission | real SQLite concurrent first-claim, matching join, conflict, expiry and stale-generation fencing are covered; opt-in real MySQL/PostgreSQL transaction suite remains the external dialect gate |
| `report_export` | Report / Scheduler / Record | Scheduler target `report_export`; file-producing asynchronous exports use Record `export` batch jobs | Scheduler `job_run` for scheduled orchestration and `record_batch_jobs` for file production | inherited from the owning Scheduler or Record claim | inherited capped retry and terminal failure evidence | inherited cancellation, expired-Lease recovery, artifact lifecycle cleanup | Report governance handoff itself is a durable state transition, not an independently executable queue |
| `download_task` | Agent/Report and Scheduler | Agent report handoff, Scheduler report target, and Record export download | durable workspace-scoped governance handoff plus Scheduler evidence; actual file chunks are produced and retained by Record batch jobs | handoff-only facts do not execute; file production inherits Record Lease/Fencing | handoff has no retry; file production inherits Record retry and terminal evidence | Record export supports cancel and lifecycle cleanup; durable handoff survives restart | no worker claim is attached to a handoff-only task, preventing a second competing export executor |

## Non-worker goroutines

- `cmd/server` runs `http.Server.ListenAndServe` in a lifecycle goroutine; it does not claim durable work.
- `platform/worker.WithHeartbeat` owns the shared per-claim heartbeat goroutine.
- Record batch execution owns one per-claim cancellation and heartbeat goroutine. It is registered above as part of `record_batch`, not as a separate business worker.
- Connector plugins do not start provider-owned polling loops; `plugins.StartBackgroundWorkers` intentionally delegates durable delivery to Integration event/outbox workers.
- Notification Inbox SSE owns request-scoped poll and heartbeat tickers; both stop with the request context, do not claim durable work, and only emit invalidation cursors.

## Reviewed raw ticker baseline

Process polling loops must use `platform/worker.StartNamedLoop`. The only reviewed production `time.NewTicker` call sites are:

- `runtime/platform/worker/lifecycle.go`: shared named loop implementation;
- `runtime/platform/worker/heartbeat.go`: shared per-claim heartbeat implementation;
- `runtime/application/record/record_batch_job_runtime_application_service.go`: Record-owned per-claim persisted cancellation plus heartbeat.
- `runtime/transport/http/notifications/notifications_inbox_stream_handler.go`: request-scoped SSE invalidation polling and heartbeat; not a process worker.
- `runtime/transport/http/records/records_stream_handler.go`: request-scoped content-free Business Workspace SSE invalidation polling and heartbeat; not a process worker.

Adding another raw ticker requires updating this inventory and its architecture gate in the same reviewed change.
